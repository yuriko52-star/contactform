# laravel11-template
git cloneしたらやること  
docker-compose.ymlにcontainer_nameを入れなおす  
docker-compose up -d --build  
docker-compose exec php bash  
composer install  
.env ファイルの作成  
cp .env.example .env  
advance-laravelらを参照
データベースが存在しているかを確認　　
画面表示する前にマイグレーションしておくこと